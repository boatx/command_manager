#!/usr/bin/env python

from distutils.core import setup

setup(name='Command Manager',
      version='1.0',
      description='Wrapper to argparser to simplify creating command line interfaces.',
      author='BoatX',
      url='https://github.com/boatx/command_manager',
      packages=['command_manager'])
